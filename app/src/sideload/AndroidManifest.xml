<?xml version="1.0" encoding="utf-8"?>
<!--
  Additions that apply only to the direct-install build.

  REQUEST_INSTALL_PACKAGES and the FileProvider exist solely so the in-app
  updater can hand a downloaded APK to the system installer. Google Play
  restricts that permission, so keeping both declarations in this source set
  guarantees they never reach the Play artifact.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application>
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.updates"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/update_file_paths" />
        </provider>
    </application>
</manifest>
